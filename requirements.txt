torch>=2.2                # Core deep learning framework for training/inference
torchvision>=0.17         # Vision models/transforms used in image pipelines
torchaudio>=2.2           # Audio tensor I/O and augmentations
transformers>=4.45        # Hugging Face models/tokenizers for multimodal inference
flask>=3.0                # Lightweight web server for the demo/web app
pillow>=10.0              # Image loading and basic image ops
scipy>=1.11               # Signal processing helpers (filters, resampling)
pydub>=0.25               # Audio decoding/encoding utilities used in preprocessing
librosa>=0.10.2           # Audio feature extraction (mel-spectrograms, etc.)
soundfile>=0.12.1         # Fast audio file read/write backend
numpy>=1.26               # Array math foundation used across pipelines
scikit-learn>=1.4         # Metrics, preprocessing utilities, classical baselines
timm>=1.0.9               # Vision model zoo/backbones (e.g., EfficientNet/ViT)
einops>=0.7.0             # Tensor rearrangements for model components
pyyaml>=6.0.1             # YAML config loading for experiments
rich>=13.7                # Better CLI logging/formatting
